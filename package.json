{
  "name": "rcs_playwright",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "bdd:gen": "bddgen",
    "clean:allure": "rimraf reports/allure-results reports/allure-report",
    "allure:prepare": "node scripts/normalize-allure-results.js",
    "allure:generate": "allure generate reports/allure-results --clean -o reports/allure-report",
    "allure:open": "allure open reports/allure-report",
    "allure:pdf": "node scripts/generate-allure-pdf.js",
    "mode:codegen": "npx playwright codegen -o src/pages/codegen.generated.ts https://dev-rcs.kopnus.com:1904/login",
    "mode:ui": "npm run clean:allure && npm run bdd:gen && playwright test --ui",


    "test": "npm run clean:allure && npm run bdd:gen && playwright test",
    "test:tag": "npm run clean:allure && npm run bdd:gen && playwright test --grep @add_tipe_dokumen",
    "test:regression": "npm run clean:allure && npm run bdd:gen && playwright test --grep @regression",
    "test:headed": "npm run clean:allure && npm run bdd:gen && playwright test --headed",
    "report": "npm run allure:prepare && npm run allure:generate && npm run allure:open",
    "report:pdf": "npm run allure:prepare && npm run allure:generate && npm run allure:pdf"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@cucumber/cucumber": "^13.0.0",
    "@playwright/test": "^1.61.1",
    "@types/node": "^26.1.0",
    "@types/pg": "^8.20.0",
    "allure-commandline": "^2.43.0",
    "allure-cucumberjs": "^3.10.2",
    "allure-playwright": "^3.10.2",
    "dotenv": "^17.4.2",
    "playwright": "^1.61.1",
    "playwright-bdd": "^9.2.0",
    "puppeteer": "^25.3.0",
    "rimraf": "^6.1.3",
    "ts-node": "^10.9.2",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "pg": "^8.22.0"
  }
}
